# Copie para ".env" e preencha com a connection string do seu projeto Neon.
# Sem essa variável, o projeto usa SQLite local (academia.db) automaticamente.
DATABASE_URL=postgresql+psycopg://usuario:senha@ep-exemplo.regiao.aws.neon.tech/academia?sslmode=require
